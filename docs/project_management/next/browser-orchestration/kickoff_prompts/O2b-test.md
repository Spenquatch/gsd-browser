# O2b-test Kickoff (Test)

## Scope
Add tests for O2b: `get_run_events` filtering/limits and `web_eval_agent` mode selection defaults.

## Role Boundaries
- Tests only. No production code. No docs/tasks edits in the worktree.

## Commands
- `uv run ruff format --check`
- `uv run pytest gsd-browser/tests -k o2b`

