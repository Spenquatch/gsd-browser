# O3a-test Kickoff (Test)

## Scope
Add tests for O3a: holder-only gating, paused-only gating, rate limiting, and payload validation.

## Role Boundaries
- Tests only. No production code. No docs/tasks edits in the worktree.

## Commands
- `uv run ruff format --check`
- `uv run pytest gsd-browser/tests -k o3a`

