# Task I6.3b: 8081 tasks endpoints: GET /api/v1/tasks and GET /api/v1/admin/tasks

## Start
- started_at_utc: 2026-01-25T00:00:00Z
- branch: feat/fastmcp-v2-tasks
- executor: codex-cli (GPT-5.2)
- task_file: docs/planning/FAST_MCP_V2_EXECUTION_TASKS.json
- prerequisites_checked:
  - make -C gsd-browser bootstrap: (not run; repo already has `.venv`)
  - make -C gsd-browser check: (pending)
- notes: Implement endpoints per `gsd-browser/docs/api/HTTP_API.md` using `gsd_browser.optionb.ops_tasks` and add tests for identity scoping, admin gating, and invalid cursor envelope.

## End
- finished_at_utc:
- commits:
  - (pending)
- commands_run:
  - (pending) make -C gsd-browser check
  - (pending) make -C gsd-browser lint
  - (pending) make -C gsd-browser test
- acceptance_criteria:
  - [ ] I6.3b.A: 8081 tasks endpoints match the pinned contract (identity scoping, admin gating, cursor errors), covered by tests.
- files_changed:
  - (pending)
